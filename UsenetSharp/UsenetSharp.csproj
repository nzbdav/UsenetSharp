<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <TargetFramework>net10.0</TargetFramework>
        <ImplicitUsings>enable</ImplicitUsings>
        <Nullable>enable</Nullable>

        <!-- Package Information -->
        <PackageId>NzbDav.UsenetSharp</PackageId>
        <Version>3.1.1</Version>
        <Authors>UsenetSharp Contributors</Authors>
        <Title>UsenetSharp</Title>
        <Description>Asynchronous, read-only NNTP access and streaming yEnc decoding for .NET 10.</Description>
        <PackageTags>usenet;nntp;yenc;networking</PackageTags>
        <PackageProjectUrl>https://github.com/nzbdav/UsenetSharp</PackageProjectUrl>
        <PackageLicenseExpression>MIT</PackageLicenseExpression>
        <RepositoryUrl>https://github.com/nzbdav/UsenetSharp.git</RepositoryUrl>
        <RepositoryType>git</RepositoryType>
        <PackageReadmeFile>README.md</PackageReadmeFile>
        <PackageRequireLicenseAcceptance>false</PackageRequireLicenseAcceptance>

        <!-- Reproducible packages, SourceLink, and symbol packages -->
        <PublishRepositoryUrl>true</PublishRepositoryUrl>
        <EmbedUntrackedSources>true</EmbedUntrackedSources>
        <IncludeSymbols>true</IncludeSymbols>
        <SymbolPackageFormat>snupkg</SymbolPackageFormat>
        <EnablePackageValidation>true</EnablePackageValidation>
        <GenerateDocumentationFile>true</GenerateDocumentationFile>
        <NoWarn>$(NoWarn);CS1591</NoWarn>
    </PropertyGroup>

    <ItemGroup>
        <!-- Include README in NuGet package -->
        <None Include="../README.md" Pack="true" PackagePath="\"/>
        <!-- Include LICENSE in NuGet package -->
        <None Include="../LICENSE" Pack="true" PackagePath="\"/>
        <!-- Include release history in NuGet package -->
        <None Include="../CHANGELOG.md" Pack="true" PackagePath="\"/>
    </ItemGroup>

    <ItemGroup>
        <PackageReference Include="Microsoft.SourceLink.GitHub" Version="10.0.300" PrivateAssets="All"/>
        <PackageReference Include="NzbDav.RapidYencSharp" Version="2.0.2"/>
    </ItemGroup>

    <ItemGroup>
        <InternalsVisibleTo Include="UsenetSharpTest"/>
        <InternalsVisibleTo Include="UsenetSharp.Benchmarks"/>
    </ItemGroup>

</Project>
